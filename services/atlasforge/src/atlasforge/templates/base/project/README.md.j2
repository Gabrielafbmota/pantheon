# {{ project_name | pascal_case }}

Generated by **AtlasForge** v{{ template_version }}

## Quick Start

```bash
# Install dependencies
poetry install

# Run development server
poetry run uvicorn src.{{ project_name | snake_case }}.presentation.api.main:app --reload

# Run tests
poetry run pytest

# Check types
poetry run mypy src/
```

## Architecture

This project follows **Clean Architecture** with 4 layers:

- **domain/** - Pure business logic (entities, value objects, ports)
- **application/** - Use cases and orchestration
- **infrastructure/** - External adapters (database, APIs, etc.)
- **presentation/** - API endpoints and schemas

## API Endpoints

- `GET /` - Root endpoint
- `GET /health` - Health check
- `GET /metrics` - Prometheus metrics (placeholder)

## Modules Enabled

{% if modules %}
{% for module in modules %}
- {{ module }}
{% endfor %}
{% else %}
No additional modules enabled
{% endif %}

---

🤖 Generated with [AtlasForge](https://github.com/yourusername/atlas-forge)
